import { Tooltip, TooltipContent, TooltipTrigger } from "@radix-ui/react-tooltip"

export const imageServer  = () => {
    return (
        <Tooltip>
        <TooltipTrigger>
            <TooltipContent>
                <p></p>
            </TooltipContent>
        </TooltipTrigger>
        </Tooltip>
    )
}